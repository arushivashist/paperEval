FROM python:3.10
RUN apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends git wget ca-certificates && rm -rf /var/lib/apt/lists/*
RUN command -v python3 || (apt-get update && apt-get install -y python3 python3-pip)
RUN apt-get update && apt-get install -y build-essential python3-dev gfortran libopenblas-dev liblapack-dev python3-pip python3-setuptools && pip3 install --no-cache-dir --upgrade pip && pip3 install --no-cache-dir numpy==1.21.2 scipy && pip3 install --no-cache-dir pandas==1.3.3 statsmodels==0.12.2 scikit-learn==0.24.2 --use-deprecated=legacy-resolver --no-build-isolation
WORKDIR /workspace
RUN useradd -m runner && mkdir -p /app/data /app/artifacts /app/tmp && chown -R runner:runner /workspace /app
USER runner
CMD ["bash"]